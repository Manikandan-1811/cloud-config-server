server:
  port: 9196

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Manikandan-1811/cloud-config-common.git


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost